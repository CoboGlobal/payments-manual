---
title: "Top-up"
lang: "en"
---

These events cover Top-up Mode, where each payer has a deposit address and every deposit is tracked on its own. Read [Events overview](/payments/en/guides/events/overview) first for the envelope, deduplication, and the rules that apply to every event.

## Events on this page

| Event | What it notifies you of | When you need it |
| --- | --- | --- |
| [`payment.topup.updated`](#payment-topup-updated) | One deposit's whole life: detected, screened, available to credit, or failed. | Always — this is the event for top-up deposits. |
| [`payment.address.updated`](#payment-address-updated) | A payer's deposit address was replaced. | Required. Besides replacements you request yourself, **Cobo also replaces addresses on its own**, and there is no other way to learn about those. |

## `payment.topup.updated`

The primary event for Top-up Mode. One deposit produces one object, identified by `transaction_id`.

**Credit the funds only when `status` is `Completed`.** There is no second condition.

### Trigger conditions

**One event per change of this deposit's `status`.** Every deposit goes through screening, so there is no direct path from `Detected` to `Completed`; `ActionRequired` returning to `PendingScreening` is the only backward edge. `Completed` and `Failed` are terminal.

| When you receive it | `status` | Creditable | What to do |
| --- | --- | --- | --- |
| The funds are detected on a deposit address | `Detected` | ❌ | Record the incoming deposit. |
| The deposit enters compliance screening | `PendingScreening` | ❌ | Wait. |
| Screening needs you to act — supply information, submit your own KYT result, or complete a manual review | `ActionRequired` | ❌ | Act on `message`. |
| Screening passed and the transaction has at least one on-chain confirmation | `Completed` | ✅ | Credit `amount`, using `transaction_id` as the idempotency key. |
| The deposit was rejected or failed | `Failed` | ❌ | Read `failed_reason.code` to decide what happens next. |

### Event content

| Field | Description |
| --- | --- |
| `transaction_id` | The deposit's transaction ID, and the object's identity. Also the parameter you pass to Cobo's compliance operations. |
| `payer_id` / `custom_payer_id` | The payer identifier assigned by Cobo, and the one you assigned. |
| `amount` | An asset amount carrying `amount`, `token_id`, and `chain_id`. Present from the first event and retained even when the deposit fails, so it tells you the size of the deposit, not whether it is usable. |
| `tx_hash` | The on-chain transaction hash. Absent for internal transfers that never touch a chain. |
| `topup_address` | The deposit address that received the funds. |
| `source_address` | The on-chain sender. `null` when the chain does not expose it. |
| `screening_mode` | Which screening system is handling the deposit: `CoboKyt`, `IntegratedVendor`, or `CustomVendor`. Use it for reconciliation and investigation. **All three modes can require action from you, so it does not tell you whether it is your turn** — `status` does. It can also be absent while screening is in progress, which means Cobo could not determine the provider at that moment, not that none is configured. |
| `message` | Present only when `status` is `ActionRequired`. Written for human operators. Three situations share that status and this field is what distinguishes them, but it is **not safe to match on**, so the three cannot be routed apart automatically and need a human queue. The three fixed strings:<br/>1. `Additional information is required…Please check the email…` — check the email Cobo sent to your account contact and supply what it asks for.<br/>2. `A screening result from your own KYT provider is required…` — submit your own KYT provider's result in the Screening App.<br/>3. `A manual compliance review by your team is required…` — complete a manual compliance review in the Screening App. |
| `failed_reason` | Present only when `status` is `Failed`. Carries `code` and `message`; values under [Failure reasons](#failure-reasons). |

To submit a screening decision, call [Submit KYT screening decision](https://www.cobo.com/developers/v2/api-references/compliance/submit-kyt-screening-decision) with the `transaction_id` from this event. Returning, quarantining, and unfreezing funds are also keyed on `transaction_id`. None of these operations accept `tx_hash`.

### Failure reasons

`failed_reason.code` is what you branch on. A deposit can carry these three. The ID to quote when you raise a case is `transaction_id`.

| `code` | What happened | What to do |
| --- | --- | --- |
| `transaction.rejected_by_screening_app` | Your own compliance team rejected this deposit in the [Screening App](https://manuals.cobo.com/en/apps/screening/introduction). | Review the case there. |
| `transaction.rejected_by_compliance` | Cobo's compliance checks rejected this deposit. | Raise it with Cobo. |
| `transaction.failed` | Everything else, including an on-chain failure and a deposit reversed by a chain reorganization. | Raise a case, quoting the ID below. |

### Sample events

Each sample shows the `data` object only. The envelope around it is the same for every event — see [Event structure](/payments/en/guides/events/overview#event-structure).

#### A deposit is detected

Show the deposit as in progress. Do not credit anything yet.

```json
{
  "data_type": "PaymentTopUpUpdate",
  "transaction_id": "aff0e1cb-15b2-4e1f-9b9d-a9133715986f",
  "merchant_id": "M1001",
  "payer_id": "P20250619T0310056d7aa",
  "custom_payer_id": "user_abc_10001",
  "status": "Detected",
  "amount": { "amount": "100.00", "token_id": "ETH_USDC", "chain_id": "ETH" },
  "tx_hash": "0xabc123",
  "topup_address": "0xtopup_addr_M1001_user_abc",
  "source_address": "0xsender001",
  "created_timestamp": 1744689000,
  "updated_timestamp": 1744689000
}
```

#### Compliance screening is in progress

Keep waiting. `screening_mode` tells you which system is reviewing it, but does not mean you have anything to do.

```json
{
  "data_type": "PaymentTopUpUpdate",
  "transaction_id": "aff0e1cb-15b2-4e1f-9b9d-a9133715986f",
  "merchant_id": "M1001",
  "payer_id": "P20250619T0310056d7aa",
  "custom_payer_id": "user_abc_10001",
  "status": "PendingScreening",
  "screening_mode": "IntegratedVendor",
  "amount": { "amount": "100.00", "token_id": "ETH_USDC", "chain_id": "ETH" },
  "tx_hash": "0xabc123",
  "topup_address": "0xtopup_addr_M1001_user_abc",
  "source_address": "0xsender001",
  "created_timestamp": 1744689000,
  "updated_timestamp": 1744689060
}
```

#### Screening needs you to act

Put the deposit in a human queue. The three possible actions are distinguished by `message` — see [When `status` is `ActionRequired`](#event-content).

```json
{
  "data_type": "PaymentTopUpUpdate",
  "transaction_id": "aff0e1cb-15b2-4e1f-9b9d-a9133715986f",
  "merchant_id": "M1001",
  "payer_id": "P20250619T0310056d7aa",
  "custom_payer_id": "user_abc_10001",
  "status": "ActionRequired",
  "screening_mode": "CoboKyt",
  "amount": { "amount": "100.00", "token_id": "ETH_USDC", "chain_id": "ETH" },
  "tx_hash": "0xabc123",
  "topup_address": "0xtopup_addr_M1001_user_abc",
  "source_address": "0xsender001",
  "message": "Additional information is required before these funds can be made available. Please check the email sent to your account contact.",
  "created_timestamp": 1744689000,
  "updated_timestamp": 1744689100
}
```

#### The funds are available

**This is the only state you may credit.** Use `transaction_id` as the idempotency key.

```json
{
  "data_type": "PaymentTopUpUpdate",
  "transaction_id": "aff0e1cb-15b2-4e1f-9b9d-a9133715986f",
  "merchant_id": "M1001",
  "payer_id": "P20250619T0310056d7aa",
  "custom_payer_id": "user_abc_10001",
  "status": "Completed",
  "amount": { "amount": "100.00", "token_id": "ETH_USDC", "chain_id": "ETH" },
  "tx_hash": "0xabc123",
  "topup_address": "0xtopup_addr_M1001_user_abc",
  "source_address": "0xsender001",
  "created_timestamp": 1744689000,
  "updated_timestamp": 1744689300
}
```

#### The deposit failed

Stop any automatic crediting and read `failed_reason.code`. Here Cobo's compliance checks rejected it, so do not resend — resolve it with Cobo first.

```json
{
  "data_type": "PaymentTopUpUpdate",
  "transaction_id": "aff0e1cb-15b2-4e1f-9b9d-a9133715986f",
  "merchant_id": "M1001",
  "payer_id": "P20250619T0310056d7aa",
  "custom_payer_id": "user_abc_10001",
  "status": "Failed",
  "amount": { "amount": "100.00", "token_id": "ETH_USDC", "chain_id": "ETH" },
  "tx_hash": "0xabc123",
  "topup_address": "0xtopup_addr_M1001_user_abc",
  "source_address": "0xsender001",
  "failed_reason": {
    "code": "transaction.rejected_by_compliance",
    "message": "Cobo's compliance checks rejected this transaction. Do not resend it — contact Cobo support to resolve the issue first."
  },
  "created_timestamp": 1744689000,
  "updated_timestamp": 1744689400
}
```

## `payment.address.updated`

Fires when a payer's top-up address is replaced. Update the address you show that payer, and keep accepting deposits to the previous address until it stops being funded.

**Event content:**

| Field | Description |
| --- | --- |
| `payer_id` / `custom_payer_id` | The payer whose address changed. |
| `chain` | The chain the address belongs to. Only that chain's address changes. |
| `previous_address` | The address in use before the replacement. |
| `updated_address` | The address to use from now on. |

Update the address you show that payer. This is a `payment.address.updated` event, not a top-up event.

```json
{
  "data_type": "PaymentAddressUpdate",
  "merchant_id": "M1001",
  "payer_id": "P20250619T0310056d7aa",
  "custom_payer_id": "user_abc_10001",
  "chain": "ETH",
  "previous_address": "0xtopup_addr_M1001_user_abc",
  "updated_address": "0xtopup_addr_M1001_user_abc_new"
}
```

## Related

- For confirmation counts, fee breakdowns, and the on-chain detail behind each deposit, subscribe to `payment.transaction.created`, `payment.transaction.completed`, and `payment.transaction.failed`. See [Advanced events](/payments/en/guides/events/advanced).
- `payment.account.balance.updated` reports the resulting balance movement for a token. See [Balance changes](/payments/en/guides/events/balances).
